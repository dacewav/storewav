# 2026-04-20 — Sesión

## Resumen
- Clonado repo dacewav/store, audit profundo de la tienda
- Identificados 15+ bugs visuales (related beats no navegaban, observer reveal dinámico, touch targets, keyframes faltantes, etc.)
- Fix loader: intento de hacerlo data-driven (esperar Firebase) — causó lentitud
- Fix loader overlap: transition → CSS animation
- Problema real: diseño SSR vs cliente — el usuario veía diseño completo brevemente y luego simplificado
- Firebase con settings vacíos = store se ve simplificado (sin links, sin stats, sin section divider)
- Revertidos TODOS los cambios del día a commit `687b860` (Bloque 5 + Pulido s8)
- Docs actualizadas timestamps

## Commits revertidos
- Todos los commits de `fdd7f59` a `179f341` fueron force-pushed fuera
- El repo quedó en `687b860` + `073532d` (docs timestamp)

## Lecciones
- NO tocar diseño sin que el usuario lo pida explícitamente
- El loader data-driven con polling agrega latencia — no hacerlo sin confirmar con el usuario
- Firebase settings vacíos = store simplificado, no es un bug del código
- El usuario valora que el store se vea como los screenshots detallados (con todos los settings de Firebase)
